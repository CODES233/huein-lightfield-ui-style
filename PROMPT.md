# 给 AI 的一键复用提示词

把下面整段复制给 AI（Claude Code / Cursor / WorkBuddy / ChatGPT 都行），
方括号里的内容按你的情况替换。

---

## 用法 A · 技能已放进 AI 的技能目录（推荐）

```
请使用技能 huein-lightfield-ui-style，把我这个项目改造成「悬浮岛 + 光场」风格。

目标项目：[项目路径，或直接把代码贴进来]
技术栈：[例如 Vite + React / 原生 HTML + CSS / Vue 3 + Tailwind / PHP 服务端渲染]
现有界面：[一句话，例如「左侧固定菜单 + 顶部工具栏 + 内容区的后台」]
不要动的地方：[例如「表单页的字段布局」「已有的数据表格」]

执行要求（重要，请逐条遵守）：
1. 先完整读技能根目录的 SKILL.md，再按它的「从零复刻 · 七步」执行；
2. 每一步动手之前，先读那一步指定的 references/ 文件，不要凭记忆写代码；
3. 按顺序做，不要跳步 —— 后一步假设前一步已经就位；
4. 一次只改一小块，每改完一块停下来告诉我改了什么、动到哪些文件；
5. 第 1 步（定 token）做完后先让我确认配色，再往下走；
6. 每一步做完后按 references/05-verify.md 的方式自检 —— 写成断言，不要只说「看起来对了」；
7. 如果我的项目里有和技能冲突的写法（比如已经在用 clamp() 流体字号、
   或遮罩层被放在内容区里），先说明冲突和风险，再改；
8. 全部做完后给我两份清单：①「改了哪些文件、哪些新增哪些覆盖」
   ②「我没能做到 / 需要你决定的事」。
```

---

## 用法 B · 只拿到一份文本（读不到 `references/`）

把仓库里的 `SKILL.full.md` 整份粘进去，加上这段：

```
我下面给你一份前端设计风格的复刻手册（含入口 + 五份参考文档）。
请把它当成技能来用：
1. 先通读，然后用你自己的话复述一遍「这套风格的七步顺序」和「三条铁律」，
   让我确认你读懂了再动手；
2. 然后把我下面的项目按它的顺序改造：[项目路径 / 代码]；
3. 每一步动手前，回到手册里对应那一节，照里面的做法写，不要凭印象写；
4. 手册里标 ⚠️ 的地方是真实踩过的坑，不要跳过；
5. 遇到手册没覆盖的情况，明确说「手册里没有，我的判断是……」，不要含糊过去。

---
以下是手册全文：

[粘贴 SKILL.full.md]
```

---

## 用法 C · 最省事的一句（先看效果）

```
用技能 huein-lightfield-ui-style（或我给你的那份手册）把下面这句话
变成一个可运行的单文件 HTML 演示页，做完直接把完整 HTML 给我，我打开就能看：

「一个暗色后台，左边是可收起的图标栏，顶部一条悬浮工具栏，内容区三张统计卡。
鼠标移动时卡片边缘亮起，背景有三团缓慢漂移的光，还叠着一层点阵背景。」
```

---

## 为什么这样写

那几条要求不是客套话，**少一条效果就会差很多**：

| 要求 | 不加会怎样 |
|---|---|
| 「每一步先读 `references/`」 | AI 会凭印象写 CSS —— 漏掉三层光的镜像关系、解析期状态、`fixed` 包含块这些关键点 |
| 「按顺序，不要跳步」 | 会把光和动效一次性铺开，出问题时无法定位是哪一步引入的 |
| 「写成断言，不要只看」 | 动效「看着对」是最容易骗人的：实际常出现落地暗缝、首帧闪一下、边缘光永远不亮 |
| 「冲突先说明再改」 | 它可能默默删掉你原有的布局约定，改完你才发现 |
| 「一次只改一小块」 | 一次性大改之后，你只能整份回滚，没法只退回某一步 |

## 如果你自己就是那个「AI」

（给 agent 看的）这套风格里最容易做错的三件事，先记住再开工：

1. **三层光场是「围绕焦点做镜像」，不是三个固定锚点。** 写错成固定锚点的后果是
   三团光全挤在屏幕上方一条横带、下半屏纯黑 —— 而且**参数都在、不报错、注释还理直气壮**。
2. **光由「距离」决定，不是「碰到才亮」。** 距离要用「到**矩形本身**的精确距离」。
3. **生硬通常出在「时序」，不在曲线。** 凡是「A 已到位、B 还在路上」，调 B 的曲线没用 —— 要把 A 按住。
